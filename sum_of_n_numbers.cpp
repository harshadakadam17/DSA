#include <iostream>
using namespace std;

int main()
{
    int n = 5;
    int sum = 0;
    int count = 1;
    sum = sum + count;
    if (sum = sum + count <= n)
    {
        cout <<sum;
    }
    else
    {
        cout << sum;
    }

    return 0;
}